# Versioning

V1.1 uses product and npm package version `1.1.0`, iOS marketing version `1.1`, and wire protocol `2`. The iOS build number increases for every uploaded or superseded binary.

Protocol 2 is a clean boundary. A protocol-1 request is rejected, not imported, relabeled, or partially rendered.
